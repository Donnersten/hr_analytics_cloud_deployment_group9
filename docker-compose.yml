version: "3.9"

services:
  dwh_pipeline:
    platform: linux/amd64
    build:
      context: .
      dockerfile: dockerfile.dwh
    ports:
      - "3000:3000"
    image: crhrproject.azurecr.io/hr-project-pipeline:latest

  dashboard:
    platform: linux/amd64
    build:
      context: .
      dockerfile: dockerfile.dashboard
    ports:
      - "8501:8501"
    image: crhrproject.azurecr.io/hr-project-dasboard:latest